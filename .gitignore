node_modules
.next
out
.env*
!.env.example
*.log
.DS_Store
next-env.d.ts
.vercel

# Certificates / private keys — never commit
*.pfx
*.p12
*.pem
*.key
*.cer
*.crt

# TypeScript incremental build info
tsconfig.tsbuildinfo

# UAT seed config — TEST emails + pay rates, never committed (#511); .example is the template
scripts/seed-time-uat.config.json
