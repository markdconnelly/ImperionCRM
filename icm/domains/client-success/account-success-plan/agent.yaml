name: account-success-plan
model: claude-sonnet-4-5
system_compose:                       # ordered; Constitution -> domain room -> Celeste persona -> workflow prose (ADR-0088 §2)
  - ../../../CONSTITUTION.md
  - ../room.md
  - ../celeste.md
  - ./prose.md
okf_rooms: [account, contact, opportunity, interaction, ticket, strategic_business_review]   # subset of domains/client-success/room.yaml
tools: [pg.read]                                                                              # read + assemble only; recommendations park
skills:
  - ./skills/success-plan-rubric        # Tier 3 — workflow-local (plan structure + signal-vs-inference)
autonomy_rung: L2
auto_may_self_approve: "assemble + refresh the internal Account Success Plan and compute the health/trajectory picture from MEASURED signals (internal, reversible) at L2 — always labeling measured signal vs Celeste's inference. Every recommendation, every binding commitment (roadmap/SLA/pricing/spend/security-remediation), and every client-facing touch in the plan parks for a human in every mode — the NO-COMMITS-EVER ceiling is dial-proof (celeste.md guardrail 1)."
