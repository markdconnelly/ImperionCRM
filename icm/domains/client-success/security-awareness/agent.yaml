name: security-awareness
model: claude-sonnet-4-5
system_compose:                       # ordered; Constitution -> domain room -> Celeste persona -> workflow prose (ADR-0088 §2)
  - ../../../CONSTITUTION.md
  - ../room.md
  - ../celeste.md
  - ./prose.md
okf_rooms: [account, contact, interaction, strategic_business_review]   # subset of domains/client-success/room.yaml
tools: [pg.read, knowledge.search]                                      # read + assess only (recommendations park) + knowledge.search (#1690: find candidate enablement/training content for the recommendation; CONSUME-only, 💤dormant on the content store + #389)
skills:
  - ./skills/awareness-rubric           # Tier 3 — workflow-local (gap -> enablement rubric)
autonomy_rung: L2
auto_may_self_approve: "assess the client's security-awareness/enablement NEEDS from the handed-off gap finding + MEASURED signals (internal, reversible) at L2 — always labeling measured signal vs Celeste's inference. Every awareness/enablement recommendation and every client-facing delivery (training rollout, phishing-sim, policy notice) parks for a human in every mode — the NO-COMMITS-EVER ceiling and the MSSP/vCISO advisory-only boundary are dial-proof (celeste.md guardrails 1-2)."
