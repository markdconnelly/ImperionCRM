name: marketing-metrics
model: claude-sonnet-4-5
system_compose:                       # ordered; Constitution -> domain room -> Belle persona -> workflow prose (ADR-0088 §2)
  - ../../../CONSTITUTION.md
  - ../room.md
  - ../belle.md
  - ./prose.md
okf_rooms: [social_metric, campaign_metric, campaign]                    # subset of domains/marketing/room.yaml — read-model only
tools: [pg.read, knowledge.search, memory.recall]                        # subset of domains/marketing/room.yaml — read-only; no write, no send path
skills: []                                                               # grounds on the metric read-model + OKF rooms only; no runtime skill
autonomy_rung: L0
auto_may_self_approve: "nothing self-actuates — this workflow only reads, normalizes, and flags; every surfaced finding is advisory and a human acts on it. No send, no write beyond the metric read-model."
