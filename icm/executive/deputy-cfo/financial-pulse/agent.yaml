name: financial-pulse
model: claude-sonnet-4-5
system_compose:                       # ordered; Constitution -> exec room -> Sterling persona -> tracer prose (ADR-0088 §2)
  - ../../../CONSTITUTION.md
  - ../room.md
  - ../sterling.md
  - ./prose.md
okf_rooms: [invoice, generated_invoice, time_record, timesheet, expense_item, expense_report, opportunity, license_assignment, account]   # subset of executive/deputy-cfo/room.yaml; only what stages ground on
tools: [pg.read, knowledge.search, memory.recall]   # subset of the exec budget; read + synthesize only — no delegate/handoff/actuation in the v1 tracer
skills: []
autonomy_rung: L1                     # propose — the pulse parks for Nick; the exec ceiling is L2 delegate-only (CONSTITUTION §9)
auto_may_self_approve: "publish the scheduled financial pulse when every section is grounded and cited; any flag, gap, or recall miss parks for Nick — finance stays read-only"
