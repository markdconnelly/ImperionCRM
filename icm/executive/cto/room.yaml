# room.yaml — CTO (Dexter) executive budget (ADR-0088 §3; Executive-Suite tier).
#
# DELEGATE-ONLY: no direct-actuation tool (gate allow-list {pg.read,
# knowledge.search, memory.recall, delegate, handoff}). Every entry ⊆ Constitution.
# No secrets, no client PII (ADR-0060).

reports_to: orchestrator   # Dexter reports to Nova (serves Derek/Mark/Luke/Brandon/Anna)

tools:
  - pg.read
  - knowledge.search
  - memory.recall
  - delegate
  - handoff

okf_rooms:
  - account
  - ticket
  - device
  - cloud_asset
  - problem                 # problem-recurrence oversight — Sage's ITIL problem / root-cause record (read)
  - known_error             # problem-recurrence oversight — known-error + workaround register (read)
  - change_request          # change-risk oversight — Marshall's typed change working object (read)
  - change_freeze           # change-risk oversight — freeze calendar; overlap = hard always_gate at Marshall (read)
  - rollback_plan           # change-risk oversight — rollback artifact + sign-off state (read)
  - standard_change_catalog # change-risk oversight — pre-authorized standard-change templates (read)
  - project                 # project-delivery oversight — Pierce's delivery portfolio (read)
